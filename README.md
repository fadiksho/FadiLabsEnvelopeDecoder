# FadiLabs envelope decoder

Private developer tool to paste `EnvelopeMessage` JSON and preview the parsed `Result` body.

Usage
1. Paste the EnvelopeMessage JSON into the left panel.
2. The right panel shows the pretty-printed deserialized `Result` body (Entity).

Notes
- Intended as a small local debugging aid for the FadiLabs project.
